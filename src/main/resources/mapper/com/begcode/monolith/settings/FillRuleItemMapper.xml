<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.begcode.monolith.settings.repository.FillRuleItemRepository">

    <!-- begcode-please-regenerate-this-file 如果您不希望重新生成代码时被覆盖，将please修改为don't ！！！-->

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="com.begcode.monolith.settings.domain.FillRuleItem">
        <id column="id" property="id"/>
        <id column="id" property="id" />
        <result column="sort_value" property="sortValue" />
        <result column="field_param_type" property="fieldParamType" />
        <result column="field_param_value" property="fieldParamValue" />
        <result column="date_pattern" property="datePattern" />
        <result column="seq_length" property="seqLength" />
        <result column="seq_increment" property="seqIncrement" />
        <result column="seq_start_value" property="seqStartValue" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id,
        sort_value,
        field_param_type,
        field_param_value,
        date_pattern,
        seq_length,
        seq_increment,
        seq_start_value
    </sql>

</mapper>
