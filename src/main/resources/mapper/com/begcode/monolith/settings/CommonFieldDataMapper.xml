<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.begcode.monolith.settings.repository.CommonFieldDataRepository">

    <!-- begcode-please-regenerate-this-file 如果您不希望重新生成代码时被覆盖，将please修改为don't ！！！-->

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="com.begcode.monolith.settings.domain.CommonFieldData">
        <id column="id" property="id"/>
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="value" property="value" />
        <result column="label" property="label" />
        <result column="value_type" property="valueType" />
        <result column="remark" property="remark" />
        <result column="sort_value" property="sortValue" />
        <result column="disabled" property="disabled" />
        <result column="owner_entity_name" property="ownerEntityName" />
        <result column="owner_entity_id" property="ownerEntityId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id,
        name,
        value,
        label,
        value_type,
        remark,
        sort_value,
        disabled,
        owner_entity_name,
        owner_entity_id
    </sql>

</mapper>
